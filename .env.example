# ============================================================================
# Bulwark 环境变量模板
# 复制本文件为 .env 并根据实际环境填写
# 所有 BULWARK_ 前缀的变量会被 ConfigLoader 自动读取
# ============================================================================

# ---------------- 会话配置 ----------------
# 会话超时秒数（默认 2592000 = 30 天）
BULWARK_TIMEOUT=2592000

# 活跃超时秒数（-1 表示跟随 timeout，>0 表示无活动 N 秒后失效）
BULWARK_ACTIVE_TIMEOUT=-1

# 同账号多端共享会话（true / false）
BULWARK_IS_SHARE=true

# 允许同账号并发登录（true / false）
BULWARK_IS_CONCURRENT=true

# ---------------- Token 配置 ----------------
# Cookie / Header 名称
BULWARK_TOKEN_NAME=bulwark-token

# Token 风格：uuid / random-64 / simple / jwt
BULWARK_TOKEN_STYLE=random-64

# 未登录时抛异常（true）或返回 false（false）
BULWARK_THROW_ON_NOT_LOGIN=true

# ---------------- JWT 配置（protocol-jwt feature） ----------------
# JWT 签名密钥（HS256/HS512 共用，生产环境必须修改）
BULWARK_JWT_SECRET=change-me-in-production

# JWT 算法：HS256 / HS512
BULWARK_JWT_ALGORITHM=HS256

# ---------------- 签名配置（protocol-sign feature） ----------------
# 签名时间窗口秒数（超过此窗口的请求被拒绝）
BULWARK_SIGN_WINDOW_SECONDS=300

# ---------------- SSO 配置（protocol-sso feature） ----------------
# SSO ticket 有效期秒数（默认 60 秒）
BULWARK_SSO_TICKET_TTL_SECONDS=60

# ---------------- 数据库配置（db-sqlite feature） ----------------
# SQLite 数据库文件路径
BULWARK_DB_URL=sqlite://bulwark.db?mode=rwc

# ---------------- 缓存配置（cache-redis feature） ----------------
# Redis 连接 URL（cache-redis feature 启用时）
BULWARK_REDIS_URL=redis://127.0.0.1:6379/0

# ---------------- 日志配置 ----------------
# 日志级别：error / warn / info / debug / trace
RUST_LOG=bulwark=info

# ---------------- 可观测性（可选 feature） ----------------
# 启用 tracing 日志桥接（tracing-log feature）
BULWARK_TRACING_LOG=false

# 启用 Prometheus 指标（metrics-prometheus feature）
BULWARK_METRICS_PROMETHEUS=false

# Prometheus 指标暴露端口
BULWARK_METRICS_PORT=9090
