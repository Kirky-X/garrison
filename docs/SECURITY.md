# Bulwark 安全政策

Bulwark 是一个面向 Rust 生态的身份认证鉴权框架，安全是项目的核心价值之一。本文档说明支持的版本范围、漏洞报告流程、使用最佳实践及已知安全考量。

> **协议与许可证**：项目以 Apache-2.0 协议开源。安全漏洞报告与披露遵循本文件描述的协调披露流程。

---

## 目录

- [支持的版本](#支持的版本)
- [报告漏洞](#报告漏洞)
- [安全最佳实践](#安全最佳实践)
- [已知安全考量](#已知安全考量)

---

## 支持的版本

Bulwark 仅对已发布版本提供安全修复，开发分支（`main`）不承诺向后移植补丁。

| 版本 | 支持状态 |
|------|---------|
| 0.1.x | ✅ 维护中 |
| < 0.1 | ❌ 不支持 |

> 0.2.0 发布后，0.1.x 将进入仅修复严重漏洞的「有限维护」状态，至少持续 6 个月。

---

## 报告漏洞

### 请勿公开提交 Issue！

**严禁通过公开的 GitHub Issue、Pull Request、Discussions 或任何公开渠道披露安全漏洞。** 公开提交可能导致漏洞在被修复前被利用，危害所有用户。

### 报告渠道

请通过以下任一私密渠道报告：

1. **首选 — 邮件**：发送邮件至 `security@kirky.dev`
2. **次选 — GitHub Security Advisory**：通过 [GitHub Security Advisory](https://github.com/Kirky-X/bulwark/security/advisories/new) 提交（建议使用，支持加密通信与 CVE 申请）

### 报告内容要求

为便于快速评估与修复，请尽量在报告中包含：

- **漏洞描述**：清晰说明漏洞类型与本质（如越权访问、令牌伪造、会话固定等）。
- **受影响版本**：明确标注你测试时使用的 Bulwark 版本与启用的 feature 组合（如 `--features "full"` 或 `--features "production"`）。
- **复现步骤**：
  - 最小可复现代码片段或测试用例。
  - 相关配置（`bulwark.toml` 片段，注意隐去敏感信息如密钥）。
  - 运行环境（OS、Rust 版本、依赖版本）。
- **影响范围评估**：
  - 攻击者可达成什么（如越权读取、伪造身份、DoS）。
  - 触发条件（是否需要登录、是否需要特定配置）。
  - 预估受影响用户范围。
- **期望修复时间**：你认为合理的修复时限（参考下方响应承诺）。
- **建议修复方案**（可选）：如果你已研究过修复方向，欢迎一并提出。

### 响应承诺

| 阶段 | 时间窗口 | 行动 |
|------|---------|------|
| 确认收到 | 48 小时内 | Maintainer 确认收到报告，并指派负责人 |
| 初步评估 | 7 天内 | 评估漏洞等级（Critical / High / Medium / Low）与影响范围 |
| 修复计划 | 14 天内 | 与报告者沟通修复方案与发布计划 |
| 修复发布 | 90 天内（依据严重程度调整） | Critical/High 优先紧急发布 patch 版本 |

修复发布后，将在 GitHub Security Advisories 中公开致谢报告者（如同意）。

---

## 安全最佳实践

在使用 Bulwark 构建生产系统时，请遵循以下安全实践。

### JWT 密钥

- **必须使用强随机字符串**，长度 **≥ 32 字节**（256 位）。
  ```bash
  # 生成 32 字节随机密钥（base64 编码）
  openssl rand -base64 32
  ```
- 禁止使用弱密钥（如 `secret`、`123456`、项目名等）。
- 禁止在多个环境间复用同一密钥（开发/测试/生产必须独立）。

### 缓存后端选择

- **生产环境必须启用 `cache-redis`** 而非 `cache-memory`。
  - `cache-memory` 基于 moka 进程内缓存，进程重启即丢失，多实例部署时无法共享会话状态，存在会话不一致与登出失效风险。
  - `cache-redis` 通过 oxcache 接入 Redis，支持分布式部署与会话共享。
- 推荐生产配置：`--features "production"`（已默认包含 `cache-redis`）。

### 环境变量管理

- `BULWARK_JWT_SECRET` 环境变量 **不得硬编码到代码**或写入版本库的配置文件。
  - ✅ 正确：通过环境变量注入、密钥管理服务（如 Vault、AWS Secrets Manager）或 `.env` 文件（且 `.env` 已被 `.gitignore` 排除）。
  - ❌ 错误：在 `bulwark.toml` 或源码中明文写入 secret。
- 检查项：提交前确认 `git diff` 中无密钥泄露，CI 应配置 secret 扫描（如 git-secrets、trufflehog）。

### TLS 终止

- **HTTPS 必须由反向代理终止**（如 Nginx、Caddy、AWS ALB），Bulwark 本身不处理 TLS。
  - Bulwark 的 Web 适配（axum/actix-web/warp）监听 HTTP，由前置代理负责 TLS 卸载。
  - 确保反向代理与 Bulwark 之间为可信网络（同主机、内网或 Service Mesh mTLS）。
  - 反向代理应强制 HSTS、禁用旧版 TLS（≤ 1.1）。

### 密钥轮换

- **定期轮换 API Key 与 JWT 密钥**：
  - JWT 密钥建议每 90 天轮换一次，发现疑似泄露立即轮换。
  - 支持多密钥并存过渡：新密钥签发 + 旧密钥仅验证，过渡期 ≥ 7 天后下线旧密钥。
  - API Key 建议每 180 天轮换，长期未使用的 Key 应主动吊销。

### 其他

- **最小权限原则**：通过 RBAC 配置时，仅授予完成业务所需的最低权限，避免通配权限（如 `*`）。
- **审计日志**：启用 `listener` 与 `tracing-log` feature，记录认证/授权关键事件，便于事后追溯。
- **会话超时**：合理配置 session timeout，避免过长有效期的会话令牌。

---

## 已知安全考量

以下为 Bulwark 当前已知的安全相关考量点，使用时请评估是否影响你的业务场景。

### 1. HTTP Digest MD5 算法已不安全

- **说明**：HTTP Digest 认证默认使用 MD5 哈希算法，MD5 已被证明存在碰撞攻击，不再具备密码学安全性。
- **影响**：`secure-httpdigest` 模块若使用默认 MD5，可能受到碰撞攻击或离线字典攻击。
- **建议**：使用 **SHA-256** 替代 MD5。配置 Digest 实现时优先选择 SHA-256 算法变体。
- **状态**：将在 0.2.0 中将默认算法切换为 SHA-256，并标记 MD5 为 deprecated。

### 2. API 签名 nonce 防重放窗口

- **说明**：`protocol-sign` 模块的 API 签名机制通过 nonce + 时间戳防重放，默认时间窗口为 **300 秒**。
- **影响**：在 300 秒窗口内，攻击者若截获合法签名请求，可重放该请求。对于幂等性不足的接口（如扣款、发消息），重放可能造成业务损害。
- **建议**：
  - **高 QPS 场景**：适当缩短时间窗口（如 60 秒），但需同步放宽客户端与服务端时钟偏差容忍度。
  - **关键写操作**：在业务层补充幂等性控制（基于业务 ID 去重），不完全依赖 nonce 防重放。
  - **缓存一致性**：nonce 缓存需具备跨实例共享能力（使用 `cache-redis` 而非 `cache-memory`），否则多实例部署下防重放失效。
- **状态**：时间窗口可通过配置调整，未来将支持 per-endpoint 的窗口配置。

### 3. is_share=true 共享会话风险

- **说明**：当会话配置 `is_share=true` 时，同一账号在多端（如 Web、App、小程序）共享同一会话令牌，任意一端登录即获得其他端的会话。
- **影响**：
  - **会话劫持风险**：攻击者一旦在任一端窃取会话令牌，即可在所有端冒充该用户。
  - **登出联动**：一端登出会导致所有端同时失效，可能影响用户体验。
  - **并发续期竞态**：多端并发续期可能触发会话状态不一致。
- **建议**：
  - **高风险场景（金融、支付）**：禁用 `is_share`，使用 per-device 独立会话。
  - **需要共享的场景**：搭配 `secure-totp` 二次验证、设备指纹校验，降低劫持影响。
  - **监控**：对 `is_share=true` 配置下的会话异常（如短时间多 IP 登录）启用告警。
- **状态**：0.2.0 计划引入「最大并发会话数限制」与「异地登录二次验证」机制，缓解共享会话风险。

---

如对本文档有任何疑问或建议，请通过 [GitHub Discussions](https://github.com/Kirky-X/bulwark/discussions) 提出（非漏洞类）；安全漏洞请遵循 [报告漏洞](#报告漏洞) 流程私密提交。
